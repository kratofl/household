name: ${PROJECT_NAME}

services:
  identity-api-db:
    image: postgres:18.0
    container_name: identity-api-db
    environment:
      POSTGRES_DB: ${IDENTITY_API_DB_DATABASE}
      POSTGRES_USER: ${IDENTITY_API_DB_USER}
      POSTGRES_PASSWORD: ${IDENTITY_API_DB_PASSWORD}
    ports:
      - "${IDENTITY_API_DB_PORT}:5432"
    volumes:
      - identity-api_db_data:/var/lib/postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d postgres"]
      interval: 30s
      timeout: 10s
      retries: 5
    profiles: ["core","identity"]

  budget-api-db:
    image: postgres:18.0
    container_name: budget-api-db
    environment:
      POSTGRES_DB: ${BUDGET_API_DB_DATABASE}
      POSTGRES_USER: ${BUDGET_API_DB_USER}
      POSTGRES_PASSWORD: ${BUDGET_API_DB_PASSWORD}
    ports:
      - "${BUDGET_API_DB_PORT}:5432"
    volumes:
      - budget-api_db_data:/var/lib/postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d postgres"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - budget-api
    profiles: ["budget"]

volumes:
  budget-api_db_data:
  identity-api_db_data:

networks:
  backend:
  budget-api:
